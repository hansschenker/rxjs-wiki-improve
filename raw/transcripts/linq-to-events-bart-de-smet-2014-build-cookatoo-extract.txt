Transcribed with Cockatoo


This presentation was recorded at Build Stuff 2014 in Vilnius. This audio file and others like it are available for free download at infocue . 

com. 

Very good afternoon, everyone. Welcome to my talk about cloud -scale reactive processing using Rx. I'm Bart de Smet. I'm a principal software engineer at Microsoft working in the Bing division. And to get started, I'd just like to talk very briefly about who I am and what I do, because that will allow you to sort of see the patterns in the work I'm doing. So first of all, I'm actually Belgian, so if you recognize some accent here, it ought to be a Flemish accent from the northern part of Belgium, a little bit Dutchy. 

I used to work or used to study better at the University of Ghent. I also was an MVP for Visual C Sharp for several years. And because I don't drink Belgian beer, I went into exile into the United States. in 2007 to work on the . NET framework team, more specifically on the Windows Presentation Foundation UI framework. Later I moved into the SQL division, which is where all the reactive goodness started under the eye of Eric Meyer, who I worked with. 

And then later in 2012, I actually moved to the online services division in Bing. to build a cloud -scale event processing system that is completely based on reactive extensions and actually powers a variety of experiences, including Cortana, the personal digital assistant that we have in Windows Phone. In addition to that, I'm an author of a couple of books, C Sharp Unleashed by SAMS, and I also have a couple of courses on Pluralsight. I'm also a speaker at various conferences, and you can find a lot of videos about Reactive, C -sharp, and those kind of things on Channel 9. So in case you get lost in some of the explanations of Reactive and want to get back to the basics, we have a lot of great videos over there. And finally, and that may be the most surprising thing of this slide, I'm also a lover of everything to do with physics. 

And physics is really the way for me get inspiration about how to build things that have rigid dualities, rich symmetries, beautiful APIs, beautiful designs. And you will see that in a lot of the work we've been doing, there's some influence of the world or the domain of physics. In fact, this diagram over here is a Feynman diagram from quantum electrodynamics, which is invented by Richard Feynman. So let's get started by talking about reactive extensions very briefly. actually briefly, more like 10 to 15 minutes, to set the scene and see the kind of things we had to change in order to make Reactive scale all the way to the cloud and the devices. 

Let's get started by introducing the story of how we came to discover, in fact, Reactive Extensions. It wasn't an intentional process to build something linked to events. It just emerged out of some other work we were doing. And the context for that work was actually something called the Cloud Programmability Team within SQL Server. which was led by Eric Meyer and co -founded by Brian Beckman, and it was codenamed Tesla, after Nikola Tesla, not after the electric car company, because that one wasn't around at that time yet, or at least not visibly. So this was done in the mid -2000s, and we tried to answer some questions about, if as a company we go into cloud programming, how do we deal with the programming model for applications in the context of cloud? 

And so we had a lot of projects to that extent, that played in this arena of doing cloud -scale programming. We had things like cross -compilers from IELTS to JavaScript, so you could take any C -sharp and Visual Basic program and run it in JavaScript, which was a pretty fun project to do. We built extensions to JavaScript, which was a very popular thing to do back in the day, and it still is actually, so built supersets of languages, which is largely superseded now by languages like TypeScript. We built some distributed build system for the cloud, and we also built, accidentally, without realizing it, linked to events, which later become reactive extensions. So what motivated having that whole potpourri of four different projects under just a team of four or five people? 

Well, the answer was that we had this grand vision of building something called a tier -splitting approach to building applications, and that was code named Volta. And the Volta project really boiled down to allowing people to write single tier applications and annotate a wide range of things in the application using attributes like run on client, run on database, and so on. And we would take your application apart into pieces and distribute across different tiers of the system. And so in practice, that meant that we had to compile, for example, IL code to JavaScript in case we landed in a web browser that didn't have Silverlight or the Common Language Runtime. So that's where IL to JavaScript came from. Now, in addition to that, and that's where the fun with Reactive starts, is that we realized, well, in order to do tier splitting across different tiers, the database tier, the middle tier, which is really the cloud scale tier, and the client tier, we need to deal with asynchrony. 

And the web is very asynchronous, but we didn't have any abstractions in the . NET framework back in the day to deal with asynchrony across different components. We had begin and invoke. We had continuation passing style. It was all kind of cumbersome, and we didn't have task and task of T. So we had to invent something of our own. And here's actually a very good example of the problem we kind of ran into. 

Say you have a client application, or a cloud application rather, where you want to have mouseMoved on the client, and you want to annotate that. I want to have mouseMoved as an event on the client, which is interoperating with the DOM. And then on the server, I want to do a plus equals to attach an event handler to mouseMoved. Now we have the problem on how those events flow from the client onto the server and backwards. We need to have this subscription management going across the server. And we didn't have a good abstraction for that. 

So we wanted to have something that makes it easy to reason about splitting an application in a very algebraic way. We have one place where the different tiers meet, we draw a line between that, we have something on the left, something on the right, and sort of the composition of those two parts is the original application again. So the key problem that we realized was actually hurting us, was that events are not first -class objects. You can't talk about an event as an object and transport it across different tiers of an application. So we had to invent a first -class representation of events. Now, what does it mean to be first class? 

It simply means that you can lift a construct in the language up into the space of objects so you can pass them around. And we already know what that is. If you have things like delegates, well, delegates are carrier pigeons, objects that encapsulate the concept of a method call. So you can transport methods by lifting them into delegates. Unfortunately, for things like events and indexes and properties, constructors, finalizers, all of those things, we don't really have a first -class representation that you can pass around. You can't pass around an event from system A to system B. So we had to invent that, and that actually led us to inventing this thing called IObservable and IObserver, which is largely inspired by the observer pattern in the Design Patterns book. 

But we made a couple of changes to it to make it really compositional. And you will see that in the cloud infrastructure that I will be talking about, that composition is really the bread and butter of our system that we built. So I want to talk a little bit about this compositional approach. So here you have the iObservable. It's an interface that's covariant in T. That means it produces values of type T. And the one thing you can do with an observable is you can subscribe, give it an observer. which is a callback interface. 

And in return for subscribing, you get an IDisposable. And that's the way you can cancel your subscription. And you will see that this IDisposable is very important to be able to do composition over resource management, as we shall see in just a moment. The observer, then, is simply the callback interface. It has three callbacks. You can get any number of messages, including zero and an infinite number of messages, followed by an error or a completion, which is optional. 

So you can have infinite sequences that never terminate as well. So that's really the essence of Reactive. And based on this abstraction, we can now start building things like link -style query operators over iObservable of T. We can build things like WHERE and SELECT simply because we can pass in observables as first -class objects into those methods, so we can build extension methods on top of these. But it goes further than that. This idisposable notion that I briefly talked about in the context of the subscribe method actually gets all the way into lower -level primitives of the system called schedulers. And the only thing I want you to remember from this slide, the bottom part of the slide at least, is that we have an idisposable coming out of schedule. 

And you will see where that becomes important in just a moment. So let's talk a little bit more about this composition. And let's go through an example of building a merge operator that will actually merge two event sequences together. So how do we go about that? Well, first of all, we want to create a new observable that's returned to the caller. And we do that by some method called create, which every time you subscribe to it, this lambda expression will be invoked given the observer that you have to feed the merged data sequence into. 

So how do we merge two data sequences? Well, the answer is we subscribe to both of them. We subscribe to x's, we subscribe to y's, do a little bit of synchronization to retain the sequential nature of the resulting sequence so I have lock in here. But as you can see, those two subscribe methods actually return an IDisposable. And the subscribe method of the merge operator also needs to return an IDisposable. 

So how do we get an IDisposable out of that? Well, the answer is we simply create a composite disposable that contains both of them, and we return that one to the caller. So now we have composition not only of data flow, but also composition of resource management. You have a single IDisposable that when you call dispose on it, it cancels out all of the subscriptions to the sources. So very interesting approach to getting this composition for free and not to have to deal with a whole bunch of cancellation tokens and a whole bunch of nightmare kind of abstractions. So we briefly looked at schedulers a moment ago. 

I just want to introduce them because we had to change them for the cloud infrastructure which I will be talking about. Very briefly, what the schedulers do is provide a crisp distinction between physical and logical. When you have a database system like SQL Server, you have a relational engine which is layered on top of a SQL operating system. and the operating system deals with B -trees and all sorts of scans of data and so on, well, we have a very similar relationship in Rx. We have query operators, which are the logical surface, and then we have physical schedulers, where the code actually executes. Now, the way you can see a scheduler in practice is if you look at this method called return, which, given a value, produces an event sequence that will have exactly one callback for that particular value. 

Now, the question is, where do we do that callback? Do we do it on the thread pool? Do we do it on a UI message loop? That's what the scheduler is for, is to abstract over the place where the concurrency is introduced to be able to send out this value. We don't want that to be happening synchronously. So that's where the scheduler comes in, and because scheduler 

.schedule schedule returns an idisposable, we're pretty much done to build the return operator. Because when you subscribe to it, it will schedule some work. In return of scheduling some work, you get an idisposable. If you want to cancel that work, you can simply call dispose, and the whole scheduled unit of work will be taken away out of the scheduler queue. So that's the way you can actually build simple operators like return. 

One of the advantages of having schedulers is not only to abstract over where things happen, but also to abstract over time. And that's a very important concept, because everything in Rx that has to happen in the system has to pass through a scheduler. So if you can intercept the whole scheduler layer, you know all the work that needs to happen in the system, including when things have to happen, because you can schedule things based on time. If you know everything that needs to happen and you can order it, you can also run through it really quickly, as opposed to waiting for the real -time clock to update. You can mimic a whole bunch of events that may happen 10 years from now and run a simulation. You can do all of that stuff in what we call virtual time, which is very important to test things, but also to do things like historical log processing. 

You may have logs of your cloud service for one year worth of data, and you want to run over it really quickly in virtual time to analyze certain patterns. So with Rx, you can do that just using the same operators as you use them for real -time processing. So that's what the scheduler has enabled us to do. Now, a couple more things about this duality, which is one of the beauties of Rx. And the reason I want to talk about it very briefly is that in the cloud infrastructure, we actually want to honor all the principles of duality as well. So we came to the realization that observables and enumerables are basically just mirror images of each other, each other's jewel. 

And that means that whatever we learned from the world of enumerable, data processing over lists, and SQL databases and those kind of things can be applied in the world of observables as well. And so I won't go through the exact derivation of the duality here, but if you take a look just intuitively, you see that subscribe takes in an observer, while as getEnumerator returns an enumerator. So we basically just flip input and output positions and we get a mirror image of both interfaces and there's a very deep connection between the two of them. In fact, on Channel 9 we have a lot of videos explaining how to flip things in those types and actually end up with left -hand side type based on the right -hand side type and vice versa. So very important property that holds, which enables us to infer things like if we find a new query operator in the world of observables, well, it may be useful in the world of enumerables as well. 

So we can do cross -fertilization across those domains. One more thing is one versus many. When we introduced Task and Task of T later on, a couple of years after introducing Rx, we came to the conclusion that those things are just specializations of the zero -value and the one -value case of asynchronously producing messages. In fact, the world looks like this. You can do synchronous data processing that returns one value, and that's just called a function. You call a function, and you're blocked until the function returns a value. 

Similarly, you can do a blocking enumeration over a sequence of values, which is done using the forEach keyword. So obviously, we need to have something on the right -hand side to do asynchronous programming. First of all, we have task of T, which is a way to asynchronously get the result of a computation. And you can await that using the new C -sharp 5 .0 feature, which was introduced a couple of years back. And then on the right -hand side, in the bottom corner, we have IObservable of T, which is the multivalue asynchronous way of getting data. 

You subscribe to a sequence, and you get callbacks for every single value. Task of T is just a special case. It just notifies you once about the result that came out of it. But Iobservable of T is a superset, just like you can do A plus B in the enumerable space if you're really perverse or whatever, or masochistic. You can do additions based on two arrays containing single values, and do a zip operator between the two, and all those kind of things. So you can do a lot of things in the multi -space that you can do in the single -value space as well, obviously. 

And then finally, one thing that we introduced very late in the Rx game was to build a way to customize execution plans. And this goes to capturing the user intent in a query expression data structure. And I don't know what's going on here, so if somebody has to rescue me for this microphone, so be it. But so the expression trees allow you to capture the user intent as a data structure, which means you can interpret it or execute it in a different way than just compiling the code and executing it. I think my microphone is gone. Somebody can do something about that? 

Or do I just speak up? You can hear me? OK. Code as Data enables you to intercept the execution of the event processing queries that you have. So if you take a look over here, you have pull versus push and you have code versus data. Depending on how you capture the user intent, you can do different things. 

So let's get started by taking a look in the top left corner. If you use link to objects, you just have a set of enumerators that interact with one another. And that just happens in memory using IL instructions. So that's actually pre -compiled code. You see a query over here from x in x's where f of x and then some other stuff. Now what you will see is that... 

matter whether you do things in the enumerable space or something called the iQueryable space, you end up with exactly the same syntax, and that's a property called homo -iconicity. You don't have to know how things execute, you just type the same syntax, but the bottom one over here may translate your user intent into link to SQL to compile it into transact SQL statements that it sends to a database. A good mathematician will say that on the right -hand side, you need to have two things as well. So we already invented I -observable of t, which, guess what, has exactly the same language. You can filter push -based sequences in the same way that you can actually filter pull -based sequences. And let me turn... 

There's still some battery left, it seems, so I'll keep it on. But then at the bottom over here, you have this thing, which looks a little weird, called Ike -observable. And that's not a typo. It's just a pun on homo -iconicity. Why wouldn't the type names look the same if the intent is the same, right? So like IObservable spelled with a Q is IObservable. 

And when you type a query over there, you end up expressing your query as expression trees. So we can intercept the execution plan and send it to some different service, for example, linked to Twitter. So we can do server -side filtering as opposed to doing client -side filtering. And that's really the bread and butter of the cloud infrastructure that we built, is the ability to remote query expressions to a service in a reliable manner. So let's now get started by talking about how we can take Rx to the cloud and what the lessons learned were in Bing as we were doing that. So first of all, Satya Nadella, our new CEO, is always talking about cloud -first and mobile -first. 

I'm very proud to announce that the work we've done actually fits in both camps, so we'll get a good review at the end of the year. But let's get started by some observations. First of all, we noticed that the world's us is all about events. You have things like classic event frameworks and UI programming. You have things like sensors on devices. 

I have a Windows phone here. I have a Microsoft Band, which has six or seven sensors. So my body is an event stream of a whole bunch of metrics. My heart rate, which is going up right now because I speak really fast, whether I'm sweating, which is not the case right now. So a whole bunch of things represented as events. And that also speaks to Internet of Things scenarios. 

We have thermostats and sensors in fields and all of those kind of things. You also have monitoring infrastructures for the cloud. You have things like changes to the world's information whenever a flight update happens. Why can't we know about the flight update immediately when it happens? Why do we have to crawl the whole web to find out that something has changed, which is just a wasteful set of computations? So we want to build a scalable abstraction that actually works. 

for all of those different domains by actually hiding the concrete implementation details. A sensor is totally different from a cloud event source that comes from, say, you know, a flight provider. We want to be able to do intelligent execution, not just distributed, but we want to be able to reason over the user intent to optimize all that processing happening in different places. You may have queries that stretch across sensors on the client, things happening in the cloud. We want to execute things as close to where the data is born, just as a matter of principle. But we also want to eliminate redundant computation if two people are pretty much doing the same kind of query. 

So those are some of the principles. And so when we founded this effort back in 2012, we went to the Bing organization because, well, that's the place where we have tons of data. And we can stand on the shoulders of giants that have done lots of data processing in a batch -oriented fashion and know a thing or two about how the web looks and how all those sources of data work. come into the system and how we can make the world a better place over there. So we were also lucky that six months after that, we started to work on Cortana, the personal digital assistant for Windows Phone, which required a ton load of event processing scenarios, both on the device as well as in the cloud. So when I actually go to Cortana here, let me just try this, I can ask her, what's the status of BA49? 

So that's a flight update. It's actually telling me what the current status is of the British Airways 49 flight. But I can tap over here on Show BA Updates. And this will actually create a Reactive Extension subscription in the cloud that will stream back any updates to BA 49 to my phone. And it will actually do so also based on my location sensor to figure out when I have to leave for the airport based on the location where I am. And the location is, of course, a local sensor, which is an observable of lat -long coordinates. 

So we have just gone through a whole bunch of Rx stuff that's happening in the cloud that I helped to build. So let's actually talk about how we can abstract those different streams of data in the cloud and on the device using the reactive paradigms. So let me get started by some more observations before I go into some concrete example here. But we came to the second conclusion that the device and the cloud are not that different. we can actually build the same abstractions and the same binaries even for both the cloud and the device and get a lot of benefits from both of them. So first of all, both the device and the cloud need to have very optimal ways of packing computation. 

First of all, on the phone, we want to have this reactive framework running in the background. all the time to listen to sensors. So that has to be very small because it's resident in the memory all the time, so we need to work on having a small working set. In the cloud that immediately translates to being able to host millions or maybe tens of millions, I leave the number open here because I can't give you exact numbers, but you know millions of queries easily on any particular node in the cloud. You can have millions of those little queries that need to be put on a single machine and be optimized in terms of memory utilization, CPU utilization, all of that stuff. Secondly, The reliability of computations is a very similar requirement between the cloud and the device. 

Your device can run out of battery. When you boot it again, it would be kind of sad that it's forgotten where the location was, that it's forgotten what the aggregate is of your average location between home and work. All sorts of aggregated states should not get lost in those event processing queries if you run out of battery or when the application decides to tombstone you and you have to save all the states to persistent storage. Similarly, in the cloud, it goes without saying that all those nodes in the cloud can go down and some of those subscriptions that stand in the cloud are there for days, weeks, months, maybe even years. So we cannot afford to lose anything. So here's a couple of Cortana queries, at least simplified a little bit. 

You know, in reality, they're even more complex. Well, this first one arguably is not complex. That's when you say, give me updates about the weather in Vilnius, you will actually create some query that looks like that in the cloud that will stream back weather updates to you. Here we have something that's a little bit more temporal, that's to update you about flight status, where you see that we have to do things like delay subscriptions and take until, that actually allow us to time cap the sequence of flights so that we don't have an event query sitting there in the cloud. If you have a booking of a flight six months in advance, we don't want that. or waste any resources on processing that stream. 

So you see delay subscription and take until there to time cap that. And then we have a very complex query here, which is a so -called time to leave query, which enables us to tell you based on your current location and your next meeting, when you have to leave based on the road conditions between point A and point B. So this one starts off by having a device site event stream for user location, doing some sampling and making sure that if the user doesn't move, we don't bother the cloud to ask like, please give me traffic information again. And at the bottom here, we want to delay doing all of this processing until sometime in advance of your meeting, which will be sufficient time for you to leave. And we want to project the messages that come out of this whole query, which will have to be filled in in the middle. We want to project it onto those notifications that say you have to leave now, otherwise you will not make it in time. So on the client, we have to listen to the GPS sensor. 

But how do we join the GPS sensor with traffic information? Well, the answer is that we can project your current location onto a CloudSight query. And the CloudSight query does something like get me the traffic as an observable between the current location that the client has told the cloud where the user is and the meeting location. And whenever there is a new traffic info, we need to create a new timer that will notify the user that he or she has to leave based on the traffic conditions that we have now. So we subtract from the meeting time the time it takes based on actual traffic to get there. In fact, this traffic query in and of itself is a big -ass query that does a combined latest between all the road segments that are on your route between point A and B. So it will actually compute the shortest travel time based on event streams that come from the roads that you have to travel on. 

So I didn't show that over here, but that's by itself a reactive computation. And then there's another one. of other things here, like prefixing the timers with a timer that's predefined. based on the ideal road conditions. In case there's no traffic updates, we don't want to miss notifying you. It's not because something didn't go wrong that you don't have to be told that you have to leave. 

So that's what the start with does. And we use a whole bunch of things like higher order operators, which you can learn about a lot more on Channel 9. In Rx, we have this operator called switch, which now allows us to actually switch between different streams of data as new streams of data arrive. So this is the way that we actually have the current timer that's based on the current conditions being cancelled and substituted for the next timer whenever the conditions change. So that's just a functional way of expressing this intent. And so you see it fits on half a screen to do this pretty complex thing that coordinates between client and cloud. 

And so how do we do all of that stuff? So let's talk a little bit about that now. Well, first of all, remember that all the way back, like now more than five years ago, I was in the Volta team, where we wanted to do something like tier splitting. And all the good ideas in one shape or form come back at some point. So in this case, we want to tier split a query which has parts that run in the cloud from parts that run on the device. So it's pretty much the same technique, but in a specialized domain. 

So the core abstraction that we have is this thing called IRP, which stands for IReactive Processing. And that's actually a representation of an event processing system. And you can think of the whole Bing cloud now to have a lollipop sticking out of it that says, I'm an iReactive processing cloud. So you can talk to that cloud in terms of a bunch of operations, which I will talk about in just a moment. Now, this enables us to do interoperability between IRP -compliant systems. You can have partner clouds that have IRPs. 

You can have all of our devices. You may not see it, but this device a little antenna on the back here which has an IRP sticking out. So I can send queries to this particular device using the same protocol as I sent them to the cloud. Now, inside the cloud itself, we use the Russian doll approach. of having the cloud being subdivided in many instances of IRPs. 

All the individual worker nodes are IRPs. And the worker nodes are physical machines, which are on multiple engines, which are IRPs by themselves. And so we have easily four or five layers of IRPs that sort of translate into one another. So we can actually delegate computations that come into the cloud to different sub -IRPs in the system and actually get the events sort of streaming between all of these, ultimately For example, notifying a user or pouring data from our cloud into a partner cloud and vice versa. So what did we have to do in order to make this work using Rx? Well, there's a couple of lessons learned from Rx, which I want to get into, which is first of all, that IObservable -of -T has a pretty compositional approach to doing subscriptions and resource management, but we had to change the phasing of the operators a little bit to make it more friendly towards cloud. 

Secondly, we had to revisit the way we think about subjects. I'll talk about it in just a moment. And sort of as the culmination of all of this work, we built something which I like to refer to, because I love physics, as the standard model of event processing. So what are the base elements that event processing is composed out of? We already have observables, subjects, observers, and subscriptions, but maybe there is something missing there. Maybe we have a Higgs boson that we haven't discovered yet. 

I'll show you the Higgs boson later. And then we also have a couple of concerns with regards to distributed event processing, of course. First of all, how do we talk about this subject versus another subject? We need to have some way of identifying artifacts. We need to deal with latencies, especially if we go across devices and services and across different services. 

We have latencies and asynchrony. We need to have a way to deal with reliability of both the computation, which is the state kept by operators, as well as message flow between systems. We need to have deployment friendliness. We need to have ways to do elasticity. For example, if there's a game on, like the Seahawks game is on, then we will have in our cloud many more subscriptions for people following their favorite player or their favorite team. But those subscriptions go away after the game is over. 

So we have peaks where we have more subscriptions than we had before. And we also want to have dynamic topologies. We don't want to redeploy the entire cloud if you have a new query. In fact, we have millions of standing queries that come and go every day. So we don't want to have a static topology that you have to deploy. For example, technologies like TwitterStorm are much more in the static topology camp than in the declarative specification with dynamic topologies. 

And finally, we want to have management of system resources being slightly different than the way we do that on single machines, of course, because we want to optimize CPU memory and all of those things. So what did we have to do to Rx to make this all work? Well, first of all, Rx has a bunch of query operators. We don't want to lose that. It's a very rich algebra that's very expressive. We've seen the time -to -leave query, which uses WHERE and SELECT and DISTINCT UNTIL CHANGED and a whole bunch of things. 

We want to have all of that stuff. We don't want to lose anything. And we have a bunch of additional requirements. First of all, a lot of operators have state. How do we persist the state so that we don't lose where the computation is if it's a computation that's running for many weeks or months and the machine goes down? How do we get to high density? 

I won't talk about this one here, that's implementation details, but high density is very important. We don't want to miss input events when a system is down. How can we get the replay of events that are down? at the time our machine was actually down in the cloud? And we want to have scalable egress. So in terms of doing reliability, we had a couple of options that we continuously re -evaluate the options because memory hierarchies are coming down with the invention of SSDs and all of that stuff. 

But at the time we decided on the approach, we came to the conclusion, well, the best thing we can do right now is, because a lot of operators do a lot of dirtying of state, is to have a periodic checkpointing approach where every so often we ask to checkpoint all the state of the running computation, we store it to disk, and then we can actually come back to that state and resume event processing. Now, in order to make that work, we also need to have a way of getting the ingress messages that come into a query evaluator machine to be able to acknowledge these. When we checkpoint, we want to say we no longer need to see those messages because we acknowledged all the state changes that were due to those messages. We want to be able to replay message streams from a certain sequence ID on in case we have to recover and replay the inputs. And so that gives us at least once guarantees. but we can actually do a whole bunch of things on top of that that get us to stricter guarantees. 

For example, we can deduplicate egress messages, we can have sequence IDs being persisted as soon as messages come in, so we can avoid side effects from repeating and all of that stuff, which currently for Cortana we don't have to do, but in the future we will consider that for other data processing needs within our cloud. So what that boils into is that when a query walks into the front door, which is the coordinator, the query coordinator, That thing knows where in the cloud we may have similar things that are already running. There may be 100 people interested in BA49 flights. So we want to reuse part of the computation. So the query coordinator is kind of this oracle in the system that knows where everything is on the different machines. It can split a query and distribute it to different Russian dolls inside the cloud that also speak IRP. 

All those IRPs, those execution engines, have checkpoint storage so we can save and load state. Those checkpoint storages are actually replicated across the cluster between primaries and secondaries. So when a primary fails over to a secondary, secondary becomes primary, we can reload the computation state. And then in order to communicate between different IRP systems, we need to have reliable messaging, which is an observer contract with onNext, which takes in a sequence ID, and that's a sequence ID of the message being produced. And then we have acknowledgement and replay, which allow us to say acknowledge up to this message ID and replay starting from that message ID. So that's really the way that the cloud is kind of structured. 

This doesn't imply that reliable messaging needs to be an external service. You can actually achieve reliable messaging by having an egress queue on the upstream node that's part of the checkpoint state. And then you can actually go back to your upstream caller to say replay from that point on. We can actually prune the queue and have the queue persistent checkpoint storage as well. So we don't need to have a message bus, a separate system between those two things, even though in some cases we do. Now what we had to do in order to make all of this really fly is first of all rethink I -observable -of -T. 

Because in I -observable -of -T you don't have a way to persist all the state of the computation. So when we take a look at I -observable -of -T, composition is kind of nice, but at the same time it can get quite hairy. And the reason for that is that when you call subscribe and you give it an observer, you can have a callback on the observer immediately, even before the subscribe method returns its I -disposable to you. So what if, as a result of a message arriving in the observer, you want to dispose the subscription? Hmm, now you have a problem. You don't have the subscription yet. 

You can't block either. So we have to have a way of making that a little bit easier. And so if you take a look at the take operator, which just gets n elements from the start of the subscription out of a given sequence, you get some counter there called remaining. You subscribe to access. It's still very fluent. You return a new observable, which every time you subscribe, return the disposable of the source subscription. 

But then in here, you need to do things like flow out the current message, do a subtraction of the counter. If the counter reaches zero, you want to send out an uncompleted message. And then, well, that's where the rub starts. How do you dispose this thing? Because you don't have the IDisposable yet. Well, you could create a closure, but the value could be null in case the thread that's creating the subscription has passed, and then the thread that's producing messages is already ahead of time. 

So, you know, you end up with a lot of complexity there. And the way we dealt with that in the world of Reactive also to invent something that's appropriately abbreviated as SAT, for Single Assignment Disposable, which is like a future disposable, that you can assign a disposable to at the point you get it. And so I'm using some pseudo -syntax here, but like, you can allocate a new SAT, and return the new SAT, and then you can assign to the SAT disposable the result of calling access . subscribe. And then inside the lambda, you can actually dispose the SAT. which means that if the set has been set immediately, because the subscribe has already returned at the point you reach that code, it disposes what's underneath the single assignment disposable. 

In case it's not assigned yet, the single assignment disposable just remembers that you wanted to dispose this thing, and then as soon as the assignment happens, we dispose it. So you have to sort of, you know, flip inside out to make those things kind of work. So that's one of the things that we actually wanted to change, and that also was justified by the observation that when you have those chains of query operators, we want to be able to actually visit those chains of operators, not just to say you have to stop, which is a dispose message flowing across those chains of operators, but we also want to use it for a variety of other reasons. We want to be able to go through a whole query operator graph and say, here's a logger for you, here's a performance counter for you, here's an instrumentation mechanism for you, here's a scheduler for you. We want to be able to visit all those nodes in the computation at any point in time to distribute state to them. 

And in addition to that, we can go to all the operators and ask, please save your state to the input parameter I'm giving you, or read your state from the input parameter I'm giving you. And that's checkpointing and recovery. So we wanted to change that. And then the observation came that the current Rx is really just conflating two dimensions. It's like, compactified dimensions in string theory. You have two things that have been composed that really are two more fundamental things. 

Namely that subscribe is attaching an observer and then starting the computation and dispose is stopping the computation and then detaching the observer. So we created this thing called isubscribable of t. which you can easily convert to I -observable to get the same feel back by just composing the two steps for subscribe and the two steps for dispose and hiding all the intricacies. But subscribe returns an I -subscription, which is an I -disposable, so we can detach. That's now the role of detach is the I -disposable. But it also has an except for a subscription visitor that can go to all the operators and do various operations on them, for example, to stop the computation. 

That means that we re -implemented the whole query operator library using this new approach, which allows us to have things like operators that implement a slew load of interfaces. Like, take says I'm a stateful operator. Where says I'm not stateful. You don't have to bother me to persist my state because I'm stateless. But so we can visit the query operator trees and ask everyone for information. stateful, please give me your state, I'll store it to the disk, give me your state, and so on, and then later we can actually go to the corresponding read methods to ask the operators to rehydrate their state. 

So a very simple way of extending current operators with checkpointing capabilities, but also with lifecycle events like onStart, onStop, and those kind of things. Secondly, we wanted to revisit the scheduler. The scheduler has a lot of benefits, namely that it abstracts over operating system resources and those kind of things, and it also hides the notion of time in such a way that we can intercept it for virtual time scheduling. But to evolve the schedulers we decided, well, we want to separate physical from logical, we want to have physical schedulers that own operating system resources, and then we want to have logical schedulers that you can basically get out of those physical schedulers by asking them please give me a logical scheduler that runs on your physical scheduler. And the reason we wanted to do that is because those logical schedulers represent units of work that are more coarse -grained than the individual actions that are actually happening, and they're more fine -grained than the whole scheduler and the whole operating system resources underneath it. So we can do things like taking a physical scheduler and splitting it into a 20 -80 % scheduler. 

The 20 % will not get more than 20 % of resources. The 80 % will not get more than 80%. But if the 20 % scheduler is passed, then the 80 % scheduler can actually go up to 100%. So we can actually balance out resource requirements. And those logical schedulers, you can actually subdivide further. So you have one logical scheduler for the entire IRP system, then logical schedulers for individual clusters of query operators, and so on. 

And then to do checkpointing, we can go to those logical schedulers and say, please cease doing any processing and checkpoints. you're done stopping the world for me for just a second. Then we can go to all the operators really quickly to persist all the state to an in -memory buffer, and then we can resume the schedulers. It's a little bit like garbage collectors that are suspending the execution engine very briefly to capture some state to do computations of pointers, and then we can resume it. And in practice, this only takes a few milliseconds for each checkpoint to pause very briefly, get all the state out, and then resume. And then we can, in the background, actually write all the state to disk. 

So pause and resume can now be done at units that are more fine -grained than the whole scheduler, and more coarse -grained than individual subscriptions, which would be a very chatty dialogue with individual subscriptions, especially if you have millions of them. So that's what we had to do there. And then finally, we also had to revisit the way we think about subjects. And that's one that actually it was too late to change when we realized that back in the Rx days that i -subject of T has a little secret in it, namely that we didn't have a good dual, mathematical dual of i -subject. We were kind of totally keen on having everything have a dual in the i -enumerable world, but we forgot about the i -subject. And intuitively, iSubject should be something that's dual to maybe a list of T or something. 

There needs to be something on the other side. So just very briefly, if you have iSubject, the way it's defined today, it implements iObservable and iObserver, and data can flow. Why can data flow? Because the iObservable is a covariant interface. It produces values of T. and the observer is a contravariant interface, it takes in values of type T. So you can already intuitively see that data can flow there. You have a way to pass things in and a way to get data out. 

But then if you flip the whole thing, and I can't pronounce this, it's just subject spelled backwards, and we actually do the intuitive thing, namely observable and observer are respectively IEnumerable and IEnumerator. In that case, you come to the conclusion that both of them are output interfaces, and like, what's that? Like body radiation that just produces something without sticking anything in? Like, that's not what we want, right? We want to have a way to put things in and get things out. 

If you take a look at what the problem is, is that the subject actually has an is -a relationship with an observer. A subject is an observer, while it had to be a has -a relationship, where you can say a subject contains observers that are the producers of the data. So we actually changed this around to imultisubject, which means that a subject is an iObservable of t, but it also has a getObserver method that you can walk up to to say, I want to start producing messages into you, give me an observer that I can talk to. And now many people can do that and the subject can coordinate what it means for many concurrent producers to be merged together. And in fact if you now create a dual of that, you can... And that will become setEnumerator, which takes in an IEnumerator of T. And actually, that makes a lot of sense. 

That's called an IBuffer in Interactive Extensions. So if you take a look on GitHub for Interactive Extensions, you will actually find the code for that for the dual of the multi -subject. And maybe we should now call it IMultiEnumerable or something. So that's the last change we had to make. So now to wrap up, I want to show you the end -user programming model called IRP. So what IRP is, is really a hypertriad of interfaces. 

Now here I've gone overboard a little bit. On the right hand side in the top corner is a Calabi -Yau manifold, which is a mathematical structure that's used in string theory, where we deal with many dimensions. And so a lot of inspiration kind of came from that space, because there's beautiful symmetries a lot of things there. So, first of all, I -reactive processing is an orthogonal basis, and this is the only difficult word I will use here, that actually consists of three things. It has the ability to get you proxies to remote artifacts, it has the ability to define new artifacts, and it has a way to discover artifacts, which are three orthogonal things. 

So that's basically the basis for iReactive processing. You can go to any system and say, I want to upload a computation to you. I want to define a macro for a computation in your space. Or I want to know what you can do for me. What kind of artifacts do you have? So that's discoverability. 

And now we come to the conclusion that this IRP thing actually needs to exist in a whole bunch of different combinations. So we can actually have our client talk to the cloud and sort of step -by -step translate into an execution plan. First of all, we have a distinction between sync and async. If we go all the way to the query evaluators, we don't want to have fine grained async where for every message we get a task and we have to await the next message and await it and so on. That's far too coarse grained. We may even want to have buffers of many elements. 

So like, you know, async on the client is fine because there's a network latency, but once you're in the same process, why would you do things asynchronously if you can actually batch them up and do very efficient loops and those kinds of things? Secondly, how do you talk about artifacts? In Rx you talk about an observable by means of a variable. Well a variable is not too much use if your process dies and comes back. Like how can you then talk about the thing that was there before but it was at some address and now it's at a different address? So sometimes you need extrinsic identifiers as opposed to intrinsic ones. 

Then we have code as data versus just code. In the query evaluators we want to compile stuff, but on the client we want to capture the user intent as an expression tree. So we need the weight to actually deal with that data. And then finally, we have things like reliable versus non -reliable messaging, a whole bunch of things here. So when we go to the right bottom, furthest most or farthest most quadrant in here in this hypercube, and actually we may want to have more dimensions here. If you go to that bottom one, we actually have the client interface, which is asynchronous, uses extrinsic identifiers, and uses expression trees for code as data. 

So that's one space that we actually do have the IRP interface for, and that's called IAsync Reactive Processing. So this is a client -side API. In reality, there's more dimensions to that, like is your client speaking with sequence IDs or not? Does it support batching or not? There's a whole bunch of variations you can have there. But the nice thing is that we have arrows back and forth between all of those cells in that hypercube. 

So we can actually do step -by -step translation as we go from one IRP to the other and sort of take away implementation details and introduce new concerns. For example, if we talk between two systems, we will introduce sequence IDs so we can actually acknowledge and replay between them. But inside the QEs, there's no sequence IDs. We shake them off on the boundaries. So we can actually do those translations between different spaces all the time. So now the three interfaces. 

First of all, we have composition through proxies. So let me just give you an example of how that looks. From the client's point of view, you just new up a context called the client context. Like in Link to SQL, you create a data context, and it's parameterized by a physical connection to the remote system. That could be a connection to a Windows phone using some phone device identifier. It could be something that remotes into the Bing Cloud by using a different kind of connection. 

But then once you have the context, you can walk up to it and say, give me a proxy to a remote observable and a remote observer. using some explicit identifiers. You have to name all of those identifiers. So you say, give me that observable for traffic and that observer to send out HTTP messages. And now you have those things. Well, at that point, we haven't gone through the IRP system. 

We've just lazily created local instances that represent something that may exist in the cloud or on the device. Now, on top of that, you can start composing things. And when you're done composing things, like a query expression, you will call subscribeAsync. It's an async API because there's a network boundary, and that's when we go into the remote system. So here's an example. You get the traffic and the HTTP. 

You do traffic . where the road segment is I90. You do a projection to an HTTP data. Then you do subscribeAsync. That's where we go to the cloud, and you pass in the HTTP observer, which is an observer of HTTP messages. And that thing also takes in a subscription identifier as a URI, which is the logical identity for that particular artifact. 

So you can go back to the API later and say, give me the subscription that's called mySubUri and do disposeAsync, maybe three months from now. And then we will actually cancel out that subscription. So explicit identifiers for every single artifact. That's one of the things in that hypercube that we have as a requirement on the client. So then the next thing is what happens when you call subscribeAsync? Well, basically the user intent, which was captured as an expression tree, gets rewritten in a whole bunch of ways. 

to normalize it, to take away type system requirements of the CLR and all sorts of things, we normalize it, and then we send it over the wire to the target IRP system. And the target IRP system will actually receive an expression tree that looks a little bit like this. So here's the expression tree. You see that we erase type information. So for example, HTTP data has become a structural type, so we don't have to share assemblies between the client and the service. You see that there's also a 

of grey boxes, which are actually unbound parameter expressions. So we represent all those operators as unbound parameters in an expression tree. What does that mean? Well, that you need to have a binder on the service that looks up in its catalog what those things are defined as, and then inlines them to compile it. So we pretty much use this as a standard form, a normal form, to represent computation that we can send across devices, across services, and all of that. And in fact, that also can work with TypeScript clients, where you have very limited information available about types, and we can do a lot of type inference on that stuff. 

So then you have the Definition API. So the Composition API that we just looked at allows you to create subscriptions and do all of that stuff. The Definition API just allows you to create macros, if you will, just like you can create stored procedures in a database. and those things are also identified using URIs. So you can say, give me the observable for traffic, and then based on that I will define a new observable that, given a string, returns a sequence of traffic infos. And that thing just is a parameterized observable that, given a road ID, will do a where filter. 

And at that point you can deploy that, and now people can talk about, I want to have roads by road ID, or traffic by road ID. So you can do something like getObservable of trafficByRoadId, and then parameterize it using a parameter. Now, the dirty secret is that even operators like where and select are actually not built in. They also obey to this naming scheme. Like, you know, they're just parameterized observables. What's a where? 

A where is an observable that takes in as a parameter an observable and a predicate function. So we can do turtles all the way down, use the same mechanism across the whole stack. So if you take a look at the way you write the query here, like access . where, that's just shorthand syntax for doing getObservable of a parameterized object. for where, and then doing function invocation. It's just an illusion that that's created by the client APIs. 

And so the same for select, you know, you see things growing in the opposite direction because extension methods sort of desugar into that way. But you see, this is just using the same concept as you can use yourself. So that's how those things compile. And the way that happens is by having those methods like where, have an attribute on top of them that says, like, this thing is identified on the server using this identifier, which means you can actually create your own APIs to have a method called trafficByRoadId, put that attribute on top of it, take in a string and return an IObservable of trafficInfo. And then when you call that method in an expression tree, we'll rewrite it and normalize it to that invocation pattern that we saw before. So we didn't want to have anything built in, even not the query operators. 

So what's IAsyncReactiveCobservable, the type we see all over the place here? It's async because it's client -side. That's quite logical. It's reactive because, well, we didn't want to clash at some points in that whole matrix of existing things, so we just inserted reactive in every single interface type, and we may undo that at the point we ship this as Rx 3 .0. And then finally, it's cobservable because it uses expression trees. So that's really that point in that matrix that we looked at that we're using on the client -side. 

So to wrap up, one more thing, which is the metadata. So the metadata APIs allow you to discover, just like a catalog and a database system, what's available on the other side. And why is that very important? Because it enables you to create tools that automatically generate client context with a whole bunch of things mapped in, like drag and drop from Server Explorer onto a designer surface kind of thing. But it also enables delegation. That's how we talk between the cloud and the device. 

We will ask from the cloud, I have a query here that talks about GPS locations. I don't have GPS locations, but I know you are defined in a different system. So let's ask the phone, do you support GPS locations? And that thing says, yes, I support that. So we can delegate that part of the expression tree to them. 

And now we can ask them, do you also support a where clause and a sample? Because I don't want to upload all the GPS coordinates and then sample. So we can actually discover what we can delegate. And so all of that comes together in all of those artifacts that we looked at before, observables, observers, and so on, and we actually built this new matrix of all the base primitive elements that occur in IRP that enable you to do a whole bunch of computations. And I'll leave this thing open here for you to think about, but there's something missing in a spot that corresponds to observers which are hot artifacts, there is something missing there and we will fill that in maybe in the next conference I can tell you what that thing was and by then we may have shipped. So in conclusion what's next? 

Well we're considering building this out as RX 3 .0 We are currently peeling the onion of the system because it's a pretty complex system and we want to boil things down to the essence. So what can we ship as a library that doesn't have dependencies on the Bing infrastructure and the data center? Everything is designed to be very reusable and pluggable, so it's not that big of a deal, but we want to sort of go through all of those pieces and individually see which ones make sense to actually ship externally. And we want to have feedback, like, what do you think about this programming model? Please come to talk about it with me. I will be around the whole day and tomorrow as well. I can show you some demos, give you a feel for actually how it looks and feels to use. 

But very importantly, like, do you think checkpointing recovery is something you want to have? Do you think that this remoting of capabilities is something that's interesting to scenarios that you have? Like all of that feedback, we really value as we're evaluating how we can actually get this out, this technology out into more hands out there. So with that, I don't have time for Q &A, unfortunately, but I will be around. So please bother me at any point in time. 

Including during parties because I don't drink beer I can always make sense so like even if other people start sounding kind of weird So, you know, please find me talk to me and thanks for your attention


Transcribed with Cockatoo
