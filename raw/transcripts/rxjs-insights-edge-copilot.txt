Detailed summary


## Syntax Design for FRP Languages
- Designing syntax for a Functional Reactive Programming (FRP) language involves choosing how programmers express time, change, and reactivity in a clear, composable, and aligned manner with the language's semantic laws, and this process includes deciding how users write reactive programs, such as declaring signals, defining derived values, expressing effects, and representing time-varying values.
- The syntax design for an FRP language must choose one of three broad syntactic styles: declarative signal syntax, functional combinator syntax, or stream-first syntax, each expressing a different philosophy of reactivity, and examples of these styles include signal count = 0, const doubled = map(count, x => x * 2), and const doubled$ = count$.pipe(map(x => x * 2)).
- When designing the syntax for an FRP language, it is essential to decide how time appears in the syntax, including implicit time, explicit time, or continuous time, and the chosen time model must be reflected in the syntax, such as Elm's core calculus treating signals as streams of discrete values or classical FRP treating behaviors as functions from time to values.
- The syntax for combining reactive values must be decided, including the use of operators, combinators, or implicit lifting, and examples of this include Elm's lift and foldp, or Haskell FRP's applicative syntax for behaviors, such as lift2 (+) mouseX mouseY or liftA2 (<>) edit1 edit2.

## Syntax for Combining Reactive Values
- The syntax for effects must be syntactically distinct from pure reactive expressions, with options including explicit effect blocks, subscribe calls, onChange declarations, or do-notation, and the syntax must reinforce the purity law that reactive expressions are pure and effects are not.
- A runtime contract in an FRP language defines the guarantees the engine must uphold about how reactive values update, how time flows, and how effects run, and it is the operational counterpart to semantic laws, specifying the rules the engine must follow when executing signals, memos, effects, dependency tracking, scheduling, and teardown.
- The runtime contract includes defining a strict update order, such as signals updating synchronously, memos recomputing next, and effects running last, to prevent glitches and ensure temporal consistency, and this contract is essential for ensuring that programs behave predictably, regardless of the complexity of the reactive graph.

## Runtime Contracts and Semantic Laws
- The document 'Join "Creating an RxJS Course from Insights" group with Copilot' outlines the key steps and contracts for designing a reactive programming language, specifically focusing on the effect model and its role in interacting with the outside world while preserving purity and predictability.
- The runtime implementation must enforce several contracts, including dependency tracking, purity and isolation, batching and atomicity, effect lifecycle management, teardown and memory safety, and time semantics, to ensure the graph remains accurate and up-to-date.
- The effect model is a crucial part of the language design, defining what counts as a side-effect, when effects run, how effects interact with reactive state, and how cleanup works, with the goal of protecting purity and predictability.

## Effect Model and Purity Enforcement
- To achieve this, the effect model must enforce a strict separation between the pure graph, consisting of signals and memos, and the impure layer, consisting of effects, to ensure deterministic computation, predictable scheduling, and no hidden side-effects.
- The runtime must guarantee that effects run after the graph stabilizes, with all signals updated and memos recomputed, to prevent glitches and ensure temporal consistency, and that effects re-subscribe on every run to support dynamic dependency graphs and prevent memory leaks.
- Hans is exploring the effect model for an FRP language, with Copilot guiding the discussion and introducing key concepts, such as the operational counterpart to semantic laws and the core part of the runtime contract, to help shape the mental model of the language.
- The effect model consists of several steps, including defining what counts as a side-effect, when effects run, and how effects interact with reactive state, as well as ensuring that effects are isolated, run after the graph stabilizes, and re-subscribe on every run to maintain purity and predictability.

## Introduction to Ripple Language Design
- The discussion is part of a larger conversation about designing an FRP language, with options to explore the effect model, designing a custom FRP syntax, or reactive compiler hooks, and Copilot is providing guidance and introducing new ideas to help Hans understand the concepts.
- The document "Join 'Creating an RxJS Course from Insights' group with Copilot" discusses the creation of a simple FRP language, with the proposed language being named Ripple, which is built on three principles: reactivity is declarative, purity is enforced, and time is implicit.
- Ripple has a minimal syntax with three constructs: signal, a mutable reactive cell, derive, a pure memoized computation, and effect, a side-effect that reacts to changes, with the syntax being deliberately minimal, readable, and aligned with syntax design principles.

## Ripple's Syntax and Runtime Guarantees
- The language automatically lifts operators, keeping it close to algebra and supporting semantic laws like compositionality and purity, with examples including derive total = price * quantity and derive message = "Total: " + total.
- The effect model in Ripple follows a strict lifecycle, where effects run after all computations, re-subscribe on each run, support cleanup, cannot run recursively, and cannot cause glitches, with an example being effect ticker = { const id = setInterval(() => count = count + 1, 1000) cleanup () => clearInterval(id) }.
- Ripple's runtime guarantees synchronous updates, topological recomputation, effects run last, batching, and memory-safe teardown, following the structure of runtime contract and scheduler implementation.

## Dynamic Reactive Switching and Language Comparison
- The language also supports dynamic reactive switching, where dependencies are rebuilt on each run using the rules from graph mutation algorithms, as seen in the example signal mode = "a" and derive current = mode == "a" ? streamA : streamB.
- The document "Join 'Creating an RxJS Course from Insights' group with Copilot" discusses the creation of two minimal Functional Reactive Programming (FRP) languages, Ripple and Current, where Ripple is signal-based and Current is stream-based, with the goal of designing a minimal stream-based FRP language that mirrors the simplicity of Ripple but with a push-based temporal model.

## Ripple's Standard Library and Current's Design
- Ripple includes a minimal standard library with arithmetic, string concatenation, conditionals, array/map lifting, and event sources, while everything else is user-land, and an example program in Ripple demonstrates a complete reactive program using signals, derivation, and effects.
- The creation of Current, a minimal stream-based FRP language, is inspired by RxJS, Elm, and classical FRP event streams, and is built on three principles: everything is a stream, operators are pure, and effects subscribe, with a core syntax that includes stream, derive, and effect constructs.
- Current includes a minimal set of built-in stream combinators, such as map, filter, merge, scan, and switch, which follow algebraic rules described in semantic laws, and uses a discrete time model with each event having a timestamp, guaranteed ordering, and no continuous behaviors or implicit state.
- The effect model in Current allows effects to subscribe to streams, run only when events occur, and support cleanup via unsubscribe, and the language also supports dynamic switching, demonstrated through an example program that shows event sources, pure transformations, and effectful subscriptions.
- The example program in Current demonstrates the use of streams, derivation, and effects to create a complete program, and the language is designed to be minimal and coherent, with a focus on simplicity and ease of use.

## Runtime Contracts and Language Philosophy
- The document is part of a larger discussion on creating an RxJS course from insights, and the creation of Ripple and Current is guided by the principles of FRP and the goal of designing minimal and effective languages for reactive programming.
- The conversation between Hans and Copilot guides the design of Current, with Copilot introducing ideas and concepts, such as streams, syntax design, and semantic laws, and Hans providing feedback and direction on the design of the language.
- The document "Join 'Creating an RxJS Course from Insights' group with Copilot" discusses a comparison between Ripple and Current, two minimal languages that express different reactive worldviews, with Ripple using implicit state and Current using explicit events.

## Semantic Laws and Core Principles of FRP
- The core types of Ripple and Current are signal and stream, respectively, and they have different derived values, effects, and use cases, with Ripple being best for UI state and fine-grained updates, and Current being best for event handling and async flows.
- The text also explores the idea of designing a custom FRP syntax that unifies Ripple and Current into one language, as well as formalizing the stream semantics and specifying the engine for Current.
- A simple example of a Stream-based FRP language, called Current, is provided to demonstrate its syntax and flow of events, including a stream source, pure derived stream, filtered stream, and effect that reacts to events.
- The example shows how to create a click counter with filtering, using streams and pure transformations to shape event flow, and effects to react to events, with the goal of keeping the syntax easy to absorb.
- The text also touches on the concept of semantic laws for an FRP language, which define the mathematical guarantees that make the language predictable and composable, with two pillars being a precise denotation and explicit modeling of time.

## Runtime Contracts and Engine Implementation
- The denotational law is introduced, which states that every reactive expression denotes a function of time, ensuring predictable meaning, no hidden state, and no implementation-dependent behavior, and is a fundamental principle of FRP.
- The discussion is guided by Hans and Copilot, who explore the ideas and concepts of FRP languages, including the design of custom syntax, semantic laws, and runtime contracts, with the goal of creating a comprehensive understanding of the subject.
- The document "Join 'Creating an RxJS Course from Insights' group with Copilot" discusses key laws and concepts in Functional Reactive Programming (FRP) systems, including the Continuity vs. Discreteness Law, which states that FRP systems must choose whether time is continuous or discrete, shaping the entire semantics of the language.
- The Compositionality Law is essential for refactoring, optimization, and reasoning about correctness, and it follows directly from denotational semantics, meaning that the meaning of a composite expression depends only on the meanings of its parts.
- The Purity Law separates pure reactive computation from effects, ensuring no side-effects inside reactive expressions and deterministic evaluation, which is necessary because behaviors and events are mathematical objects, while effects interact with the world.
- Other important laws in FRP systems include the Temporal Consistency Law, which prevents glitches by ensuring that all derived values reflect the same moment at any logical time, and the Causality Law, which states that outputs cannot depend on future inputs.

## Runtime Contracts: Update Ordering and Dependency Tracking
- The Fusion Laws, including map fusion, filter fusion, lift laws, and fold/scan laws, ensure that combining transformations preserves meaning and avoids redundant computation, following from FRP's functional roots.
- The concept of a runtime contract in an FRP language is also introduced, which defines the rules the engine must always follow when updating reactive values, tracking dependencies, and running effects, ensuring predictable and glitch-free behavior.
- A runtime contract specifies the guarantees the engine must uphold when executing signals, memos, effects, dependency tracking, scheduling, and teardown, and it is the operational counterpart to the mathematical semantic laws.

## Effect Model Implementation and Contracts
- The update ordering contract and dependency tracking contract are two key aspects of a runtime contract, with the update ordering contract defining a strict update order to prevent glitches and ensure temporal consistency, and the dependency tracking contract guaranteeing that reading a signal inside a computation registers a dependency and that dependencies are cleared and rebuilt on each run.
- The document 'Join "Creating an RxJS Course from Insights" group with Copilot' discusses the creation of a Functional Reactive Programming (FRP) language, specifically focusing on the effect model that defines how the language interacts with the outside world while maintaining a pure and predictable reactive graph.
- The effect model is crucial in ensuring deterministic behavior, predictable scheduling, and preventing hidden side-effects or recursive updates, and it is the operational counterpart to semantic laws and part of the runtime contract.
- The runtime must enforce several contracts, including the Purity & Isolation Contract, which states that pure computations, such as signals and memos, never run effects, and effects never run during memo recomputation, to protect the graph from impurity.
- Other essential contracts include the Batching & Atomicity Contract, which guarantees that multiple writes inside a batch produce one recomputation wave, and the Effect Lifecycle Contract, which dictates that effects must follow a strict lifecycle of registration, running, cleanup, and re-subscription.

## Memory Safety and Time Semantics
- The Teardown & Memory Safety Contract is also vital, as it ensures that unsubscribing removes all edges, scopes clean up all child computations, and cleanup functions always run, with no dangling subscribers remaining, which is essential for long-running applications.
- The Time Semantics Contract defines whether time is discrete or continuous, whether updates are synchronous or asynchronous, and whether effects run immediately or are deferred, shaping the entire mental model of the language.
- Hans and Copilot are discussing the effect model, with Hans choosing to explore the effect model for an FRP language, and Copilot explaining that the effect model defines how the FRP language interacts with the outside world while keeping the reactive graph pure and predictable.


