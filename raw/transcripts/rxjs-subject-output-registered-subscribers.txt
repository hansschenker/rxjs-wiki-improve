

const subject = new Subject<number>();

subject.subscribe (x => console. log (x) );
subject.subscribe (x => console. log (x) );

console.log ((subject as any).observers) ;

source. subscribe (subject);

I