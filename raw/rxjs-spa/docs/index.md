# rxjs-spa

A monorepo for building web single page applications **solely** with **RxJS + TypeScript**.

- Libraries live in `packages/*`
- Apps live in `apps/*`
- This documentation lives in `docs/`

## Install

```bash
npm install
```

## Run

```bash
npm run dev
```
